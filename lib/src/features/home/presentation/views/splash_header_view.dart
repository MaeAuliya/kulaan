import 'package:flutter/material.dart';

class SplashHeaderView extends StatelessWidget {
  const SplashHeaderView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
